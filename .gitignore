# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env*.local
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Keep migration files - they should be version controlled
# Ignore Supabase CLI local cache (regenerated on every CLI invocation)
supabase/.temp/

# Archive directory - keep locally but don't commit to git
archive/
scene-*.png

# Workspace / test screenshots at repo root — not committed (app images live in public/)
/*.png
/*.jpg
/*.jpeg
